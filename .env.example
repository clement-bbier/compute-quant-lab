# Copier en .env (JAMAIS committé) et remplir UNIQUEMENT ce que tu utilises.
# Les lignes vides sont OK : un connecteur sans token saute proprement sa source.

# ── Énergie (jambe réelle, historisée) ──
ENTSOE_API_TOKEN=

# ── Marketplaces GPU — jambe compute (spot). Plus on en a, plus l'indice P04 ──
# ── est robuste (anti-manipulation / anti-survivorship) et les signaux de    ──
# ── dispersion inter-venues riches. Un connecteur core/ingestion par venue.  ──
# Actifs (clé en main) :
VASTAI_API_KEY=
RUNPOD_API_KEY=
# À brancher (API self-serve publique — à implémenter, ordre de priorité libre) :
LAMBDA_API_KEY=
TENSORDOCK_API_KEY=
DATACRUNCH_API_KEY=
HYPERSTACK_API_KEY=
GENESISCLOUD_API_KEY=
PAPERSPACE_API_KEY=
CRUSOE_API_KEY=
PRIMEINTELLECT_API_KEY=
# Référence / souvent sales-gated (scraping ou contrat, pas de clé self-serve) :
#   CoreWeave · hyperscalers spot (AWS/GCP/Azure) · SF Compute

# ── Indice spot compute (source canonique Silicon Data, payante) ──
SILICONDATA_API_TOKEN=
