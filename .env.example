# Copier en .env (jamais committé) et remplir. Tokens scopés au minimum.
ENTSOE_API_TOKEN=
# Marketplaces GPU (si API tokenisée)
VASTAI_API_KEY=
RUNPOD_API_KEY=
# Indice spot compute (source canonique Silicon Data, payante)
SILICONDATA_API_TOKEN=
