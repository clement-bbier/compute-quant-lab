# Gitignored files copied automatically into every new Claude Code worktree
# (.gitignore syntax). A worktree is a fresh checkout: without this, .env isn't
# there and the connectors (ENTSO-E, Silicon Data) have no tokens.
# Ref. docs/git-workflow.md §8.
.env
.claude/settings.local.json
